// Task 1
//* Виконай додавання 0,1 і 0,2 отримуєш математично правильні відповіді.*/

let sum; // Вказуєм зімнну

sum = 0.1 + 0.2; // Присвоємо значення зміній

console.log("0.1 + 0.2 = " + sum); // Виводимо результат

// Task 2
//*Виконавши додавання рядка "1" і цифри 2 (обидві операнди повинні бути в змінених), отримаєте математично правильні відповіді.*/

// Визначимо змінні для зберігання операндів
let a = "1";
let b = 2;

// Перетворимо рядки на числа
let aNum = parseFloat(a); // *! parseFloat() метод який  обертає числове значення рядка.
let bNum = parseFloat(b);

// Додамо числа
let result = aNum + bNum;

// Виведемо результат
console.log("1 + 2 = " + result);

// Task 3
// ** Користувач вказує обсяг флешки в Гб. Програма повинна порахувати кількість файлів розміром 820 Мб, що поміщається на флешку.*/

// *?Запитаємо у користувача обсяг флешки в Гб
let volumeInGb = prompt("Введіть обсяг флешки в Гб: ");

// Перетворимо обсяг флешки в байти
let volumeInBytes = volumeInGb * 1024 * 1024 * 1024;

// Визначимо змінну для зберігання розміру файлу в байтах
let fileSizeInBytes = 820 * 1024 * 1024;

// Порахуємо кількість файлів
let filesCount = volumeInBytes / fileSizeInBytes;

// Виведемо результат
console.log(
  "На флешку об'ємом " +
    volumeInGb +
    " Гб можна помістити " +
    filesCount +
    " файлів розміром 820 Мб."
);

// Task 4
//*Користувач вводить суму грошей в гаманці і ціну однієї шоколадки. Програма виводить скільки шоколадок може купити користувач і скільки здачі у нього залишиться.*/

// Користувач водить суму грошей яка є в гаманці
let manyWalet = prompt("Введіть суму грошей в гаманці");

// Користувач водить ціну однієї шиколадки
let priceChocolates = prompt("Введіть ціну однієї шиколадки");

//Змінна для зберігання кількості шоколадок, які можна купити
let chocolatesCount;

//Змінна для зберігання здачі
let change;

// Порахуємо кількість шиколадоок які можна купити
chocolatesCount = manyWalet / priceChocolates;

//  Порахуємо здачу
change = manyWalet % priceChocolates;

console.log(
  "Користувач може купити " +
    chocolatesCount +
    " шоколадок і залишиться " +
    change +
    " гривень здачі."
);

// Task 5
//*Запитай у користувача тризначне число і виведи його задом вперед. Для вирішення завдання тобі знадобитися оператор % (залишок від ділення).*/

// Запитаємо у користувача число
// const number = prompt("Введіть тризначне число: ");

// // Визначимо змінну для зберігання результату
// let reversedNumber = "";

// // Поки число не дорівнює нулю
// if (number !== 0) {
//   // Додамо останню цифру числа до результату
//   reversedNumber += number % 10;

//   // Розділимо число на 10
//   number = number / 10;
// }

// // Виведемо результат
// console.log("Число задом наперед: " + reversedNumber);

// Task 6
// ** Користувач вводить суму вкладу в банк на 2 місяці, з процентною ставкою депозиту 5% річних. Вивести суму нарахованих відсотків.

// Запитаємо у користувача суму вкладу
// const depositAmount = prompt("Введіть суму вкладу: ");

// // Визначимо кількість місяців депозиту
// const depositTermInMonths = prompt("Кількість місяців депозиту");

// // Визанчимо процент річних
// const interestRateYear = 5;

// //Визначимо кількість міясців на рік
// const monthInYear = 12;

// // Порахуємо процентний депозит на міясяць
// const interestRateMonth = monthInYear / interestRateYear;

// // Порахуємо суму нараховних відцотків
// const interestAmount = depositAmount * depositTermInMonths * interestRateMonth;

// console.log(
//   "Якщо, сума вкладу буде  " +
//     depositAmount +
//     " на таку кількість місяців депозиту " +
//     depositTermInMonths +
//     " то тоді наша сума нараховних відсотків буде " +
//     interestAmount
// );

// Запитаємо у користувача суму вкладу
const depositAmount = prompt("Введіть суму вкладу: ");

// Визначимо процентну ставку депозиту
const interestRate = 0.05;

// Визначимо кількість місяців депозиту
const depositTermInMonths = 2;

// Розрахуємо суму нарахованих відсотків
const interestAmount =
  (depositAmount * interestRate * depositTermInMonths) / 12;

// Виведемо результат
console.log("Сума нарахованих відсотків: " + interestAmount);
